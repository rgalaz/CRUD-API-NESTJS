import { DetalleFactura.Entity } from './detalle-factura.entity';

describe('DetalleFactura.Entity', () => {
  it('should be defined', () => {
    expect(new DetalleFactura.Entity()).toBeDefined();
  });
});
